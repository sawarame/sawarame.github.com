# sawara.me ツール群仕様書 (SPEC)

本ドキュメントでは、`sawara.me` プロジェクト直下に実装されている主要な便利ツールの仕様について定義します。

## 1. パスワードジェネレーター (`src/pages/password.tsx`)
指定した条件に基づいて、セキュアなランダムパスワードをクライアントサイド（ブラウザ上）で生成するツールです。

### 主な機能・仕様
* **設定項目**:
  * **パスワードの長さ:** 1〜128文字まで指定可能
  * **作成数:** 一度に生成するパスワードの数（1〜20個まで）
  * **使用しない文字:** 指定した文字を生成されるパスワードから除外する
  * **大文字・小文字の除外設定:** プリセットとしてアルファベットの大文字、または小文字を一括して使用しないように設定可能
  * **使用可能な記号:** 指定した記号のみを使用可能にする（空欄の場合は全記号を使用）
  * **文字の重複制限:** 「同じ文字を使用する」のON/OFFにより重複を許容するか設定
  * **記号の使用:** 「記号を使用する」の全般的なON/OFF
* **URLパラメータ同期**:
  * 各設定値はリアルタイムでURLのクエリパラメータに反映され、設定状態をURLとして保存・共有が可能
  * 対応パラメータ: 
    * `len`: 長さ
    * `count`: 作成数
    * `exclude`: 除外文字（個別入力）
    * `no_upper`: 大文字を除外 (true/false)
    * `no_lower`: 小文字を除外 (true/false)
    * `symbols`: 使用可能記号
    * `same`: 重複制限
    * `use_sym`: 記号使用
  * パラメータが含まれるURLでアクセスした際、その値を初期値として読み込み、自動的にパスワードを生成する
* **アクション機能**:
  * 各パスワードごとのクリップボードへの単体コピー機能
  * 全ての生成結果をテキストファイル（`.txt`）として保存するダウンロード機能
  * ダウンロードファイル名のフォーマット: `[現在日時(YYYYMMDDHHMMSS)].txt`

## 2. テキスト保存場所 (`src/pages/text.tsx`)
一時的なメモや複数行のテキストをブラウザの `localStorage` に保存し、即座に呼び出しおよび管理ができる簡易スクラッチパッドです。

### 主な機能・仕様
* **テキスト入力と保存設定**:
  * 複数行のテキスト入力エリアを提供
  * 「エンターで保存」の挙動切り替え（ON時は `Enter` 単体で保存、OFF時は `Ctrl(Cmd) + Enter` で保存）
* **データ管理 (`localStorage`)**:
  * 保存したテキストは作成日時の記録と共に `SavedTexts` として永続化される
  * 最新のメモが最も見やすいように、一覧UIの上部から表示される
  * **ピン留め機能:** 重要なテキストをピン留めし、常に一覧の最上部に表示させることが可能（ピン留め状態も `SavedTexts` に保存される）
* **アクション機能**:
  * 各メモの個別コピーおよび個別削除、ピン留めの切り替え
  * 保存済みテキストの一括コピーと一括削除
  * 保存済みテキストすべてを `.txt` 拡張子でダウンロード（各テキストは日時ヘッダを付与してリスト化）

## 3. デバイス情報チェッカー (`src/pages/device.tsx`)
現在アクセスしているデバイス本体のネットワーク情報、画面サイズ、動作環境（UA）をパッシブに表示するツールです。

### 主な機能・仕様
* **ネットワーク情報**:
  * 専用API (`https://api.sawara.me/v1/ipaddress/`) を呼び出し、クライアントのグローバルIPアドレスを表示
  * **クロール保護機能:** Bot（クローラー）からのアクセスと検出した場合はAPI制限を行い、IPアドレスを非表示にする
* **画面サイズ**:
  * **物理解像度**: デバイス・モニタ全体のピクセル解像度 (`window.screen.width × window.screen.height`)
  * **ウィンドウサイズ**: 現在のブラウザ内の描画領域 (`window.innerWidth × window.innerHeight px`)
  * ※ブラウザのリサイズイベントを検出し、リアルタイムでサイズ値が更新される
* **ユーザーエージェント**:
  * `navigator.userAgent` に基づくアクセス元のブラウザ・OS識別情報
* **アクション機能**:
  * 値（IPアドレスや物理サイズ等）を直接クリップボードへコピーするボタンを各要素に提供

## 4. QRコード作成 (`src/pages/qr.tsx`)
入力した文字列や各用途のフォーマットに対応した情報を元に、リアルタイムでQRコードを生成するツールです。

### 主な機能・仕様
* **対応フォーマット (自動文字列生成)**:
  * **テキスト / URL**: 入力した文字列をそのままQRコード化
  * **Wi-Fi**: 暗号化方式（WPA/WEP/なし）、SSID、パスワードの入力を元に `WIFI:T:...` 形式で生成
  * **連絡先**: 名前、電話番号、メールアドレスを元に `MECARD:N:...` 形式で生成
  * **予定 (カレンダー)**: 予定名、開始・終了日時（native datetime-local）を元に複数行の `VCALENDAR` 形式で生成
  * **メール作成**: 宛先、タイトル、本文を元に `mailto:` 形式URIとして生成（iPhone等のメーラー向けに改行コードを `%0D%0A` に自動変換）
* **ロゴ埋め込み機能**:
  * 任意の画像ファイルをアップロードし、QRコードの中心にロゴとして配置することが可能（画像はブラウザ内で処理）。
  * ロゴ配置時は、読み取り精度を維持するために誤り訂正レベルを「H (High)」に設定。
  * ロゴ背後のQRコードモジュールを除去（excavate）し、ロゴがはっきりと見えるように描画。
* **QRコード描画とリアルタイムプレビュー**:
  * `qrcode.react` ライブラリのCanvasを用い、入力の変更に合わせて即座にブラウザ上で描画を更新
* **出力保存機能** ("画像を保存" / "画像をコピー"):
  * 画像ファイル (`.png`) としてローカルダウンロード保存が可能
  * モードに応じて生成される**動的ファイル名**で保存される：
    * テキスト/URL: `[現在日時(YYYYMMDDHHMMSS)].png`
    * Wi-Fi: `[SSID].png`
    * 連絡先: `[名前].png`
    * カレンダー: `[予定名].png`
    * メール: `[タイトル].png`
    * ファイル名に使用できないシステム予約文字 (`/ \ : * ? " < > |`) は安全にアンダースコア `_` に自動置換され、値が空の場合は日時形式にフォールバックする
    * 画像自体をクリップボードにコピー（`image/png` Blob）する機能を実装
    * **結果の共有機能 (Web Share API)**:
    * ブラウザが画像共有に対応している場合のみ「共有」ボタンを表示。
    * OS標準の共有メニューを呼び出し、生成されたQRコード画像を他のアプリへ直接送信可能。
    * 画面幅が狭いデバイスでは、操作ミスを防ぐためボタンを縦に並べるレスポンシブレイアウトを採用。


## 5. 日付比較ツール (`src/pages/date.tsx`)
二つの入力された日付の差分を計算し、表示するツールです。

### 主な機能・仕様
* **対応フォーマット (パース処理)**:
  * 標準的な日時フォーマット (例: `YYYY/MM/DD HH:mm:ss`, `YYYY-MM-DD HH:mm:ss`)
  * 日本語の日時フォーマット (例: `YYYY年MM月DD日 HH時mm分ss秒`)
  * UNIX TIMESTAMP (秒数およびミリ秒数の数値)
  * ※ 入力したテキストはそのまま保持され、パースに成功した場合は入力欄の下部に `YYYY/MM/DD HH:mm:ss` 形式で認識された日時が表示される仕様。
* **差分の表示**:
  * 「日時分秒」の形式 (例: "1日 2時間 30分 0秒")。ただし、値が0の単位（0日、0時間など）は表示を省略し、差分が0秒の場合は "0秒" と表示する。
  * 「総秒数」での表示。
* **アクション機能**:
  * 算出された「日時分秒」および「総秒数」の結果をそれぞれクリップボードへコピーするボタンを提供。

## 6. 写真Exif情報チェッカー (`src/pages/exif.tsx`)
アップロードした写真からカメラ情報、撮影日時、GPSなどのメタデータ（Exif情報）をブラウザ上で安全に読み取り、表示するツールです。

### 主な機能・仕様
* **Exif情報の読み取り**:
  * 選択された画像ファイルから、撮影日時、カメラメーカー、モデル名、F値、シャッタースピード、ISO感度、GPS位置情報、解像度などを抽出して一覧表示。
  * 処理はすべてクライアントサイド（ブラウザ内）で行われ、画像データがサーバーに送信されることはないため安全。
* **画像のプレビュー**:
  * アップロードした画像のサムネイルを画面上に表示。
* **対応フォーマット**:
  * 画像ファイル (`image/*`) のみ受け付け、非画像ファイルが選択された場合はエラーメッセージを表示。
* **アクション機能**:
  * 「クリア」ボタンにより、現在読み込まれている画像およびExif情報をリセットし、初期状態に戻す。
  * 各情報をクリップボードへコピーするボタンを提供。
  * GPS情報がある場合は、Googleマップの検索結果へのリンクを提供。

## 7. 画像軽量化・クロップツール (`src/pages/resize.tsx`)
ブラウザ上で画像をクロップ（切り抜き）し、WebPなどの次世代形式に変換・軽量化するツールです。

### 主な機能・仕様
* **完全ローカル処理**:
  * 全ての画像処理（圧縮、切り抜き、変換）はブラウザのCanvas APIおよびクライアントサイド向けライブラリを使用して実行されます。画像データが外部サーバーへ送信されることは一切ありません。
* **クロップ（切り抜き）機能**:
  * **自由な範囲選択**: マウス操作による直感的な切り抜き範囲の指定が可能。
  * **比率プリセット**: 1:1 (正方形), 8:5 (1.6:1), 1.91:1 (OGP), 16:9 (X ポスト), 3:1 (X ヘッダー) 等の主要なアスペクト比にワンクリックで固定可能。
  * **高解像度維持**: プレビューの表示サイズに関わらず、元画像の解像度を維持したまま切り抜きを行います。
* **最適化・変換設定**:
  * **出力フォーマット**: WebP（推奨）, JPEG, PNG, オリジナル（同形式）から選択可能。
    * ※WebP変換非対応のブラウザでは自動的にJPEGが推奨として選択されます。
  * **画質 (Quality)**: 0.1〜1.0までスライダーで調整可能。
  * **最大幅指定**: プリセット（1280px, 1080px等）のほか、任意のピクセル数を手入力で指定可能。縦長の画像であっても「幅」がこの数値に収まるようにリサイズされます。
  * **EXIF保持**: JPEG出力時のみ、元のメタデータ（カメラ情報等）を維持するか選択可能。
* **比較プレビュー (Before/After)**:
  * 圧縮前と後の画像をスライダーで左右に動かして比較。
  * **ズーム・パン機能**: 最大5倍までの拡大およびドラッグによる移動に対応し、細部の劣化を詳細に確認可能。
* **バッチ処理**:
  * 複数画像の一括アップロードと一括変換に対応。
  * 変換後の画像は個別に、または `JSZip` を用いて一括でZIPダウンロードが可能。
* **高精度リサイズ**: 比率指定クロップとサイズ制限（最大幅）を併用した際、計算誤差による1pxのズレを防ぎ、指定した目標幅に基づいた正確な解像度で出力します。

## 8. PDF画像変換 (`src/pages/pdf2img.tsx`)
PDFファイルを1ページずつ画像（PNG, JPEG, WebP）に変換するツールです。

### 主な機能・仕様
* **完全ローカル処理**:
* `pdfjs-dist` を使用し、ブラウザ上でPDFのレンダリングを行います。画像データがサーバーに送信されることはありません。
* **ページ選択**:
* 全ページ、または特定のページのみを選択して画像化することが可能です。
* **最適化・変換設定**:
* **出力フォーマット**: PNG, JPEG, WebPから選択可能。
* **最大横幅指定**: 指定した横幅に収まるようにリサイズして出力します（アスペクト比は維持）。
* **柔軟なダウンロード形式**:
* 選択されたページが1枚の場合は、画像ファイルとして直接ダウンロード。
* 複数ページが選択された場合は、`JSZip` を用いてすべての画像をZIP形式でまとめてダウンロード。

## 9. Web快適度測定ツール (`src/pages/benchmark.tsx`)
...
ブラウザ上で負荷の高い演算処理を実行し、演算性能を測定してスコア化するとともに、最新のWeb機能への対応状況を診断するツールです。

### 主な機能・仕様
* **演算性能の測定 (Web Workerによるマルチスレッド対応)**:
  * 測定の精度を高めるため、浮動小数点演算、整数・ビット演算、メモリアクセスの3種類の複合ワークロードをバックグラウンドの `Web Worker` 上で実行し、その処理速度からスコアを算出します。
* **シングルコアスコアとマルチコアスコアの同時測定**:
  * **シングルコア:** 1つのワーカーのみを起動し、単一コアでの純粋な処理能力を測定。
  * **マルチコア:** デバイスの論理コア数と同数のワーカーを同時に起動し、全コア並列処理時の総合的なスループットを測定。
* **ランク評価**:
  * 測定されたスコアに基づき、性能ランク（S〜F）を評価します。
  * **シングルコア ランク目安**:
    * **Rank S** (4000以上): 比類なき速さ。最新のハイエンド端末の中でも最高峰の処理能力です。
    * **Rank A** (2000〜3999): 快適。大抵のWebアプリがストレスなく動作します。
    * **Rank B** (1000〜1999): 実用的。一般的なブラウジングには十分な性能です。
    * **Rank C** (250〜999): 標準的。一般的なページを閲覧するのに十分な性能です。
    * **Rank D** (100〜249): 基本的。負荷の高いページでは、読み込みに時間を要する場合があります。
    * **Rank E** (50〜99): 低速。古い端末や省電力モードの影響が考えられます。
    * **Rank F** (50未満): 動作困難。現代のWebサービスを利用するには大幅な性能不足です。
  * **マルチコア ランク目安**:
    * **Rank S** (30000以上): 最上級。圧倒的な並列処理能力で、あらゆる重い作業を軽快にこなします。
    * **Rank A** (10000〜29999): 強力。複数のタスクを同時に開いても安定します。
    * **Rank B** (4000〜9999): 快適。複数のタスクを並行しても余裕のある性能です。
    * **Rank C** (500〜3999): 標準的。マルチタスクも問題なく行える性能です。
    * **Rank D** (200〜499): 最小限。並列処理が増えると動作が鈍くなることがあります。
    * **Rank E** (100〜199): 不足気味。並列処理には向かず、動作が制限されます。
    * **Rank F** (100未満): 著しく低い。並列処理の恩恵をほとんど受けられません。

* **ブラウザ機能サポート状況の診断**:
  * 演算性能の測定完了後、最新のWeb技術（画像フォーマット、API、CSS機能など）への対応状況を自動的に診断します。
  * **診断カテゴリー**:
    * **SNS・メディア閲覧**: AVIF/WebP対応、AV1デコード、Priority Hints、Object-fit、CSS @layer
    * **ビジネス・ツール**: Anchor Positioning、Popover、Compression Streams、Web Crypto、IndexedDB v3
    * **エンタメ・クリエイティブ**: WebGPU、View Transitions、Scroll-driven Animations、Offscreen Canvas、Spatial Audio
    * **システム・セキュリティ**: Passkeys、Badging API、Screen Wake Lock、File System Access
  * **アコーディオン形式のUI**: カテゴリーごとに折りたたみ可能なカードで結果を表示。各項目には「機能名」「判定（✅/❌）」「利点の解説」が含まれます。
* **UI/UX**:
  * 測定開始前は結果欄をプレースホルダー状態で表示し、レイアウトのガタ付きを抑制。
  * 測定中は「シングルコア」→「マルチコア」→「機能チェック」の順にステップを進め、進捗状況をリアルタイムにフィードバックします。
  * 演算性能の測定に影響を与えないよう、機能チェックは最後に実行されるシーケンスとなっています。
* **結果の共有・測定環境の表示**:
  * **測定環境の表示**: 使用しているブラウザ名、主要バージョン、およびOS（Windows, macOS, iOS, Android等）を自動判定して画面上に表示。
  * **結果画像の生成**: 共有用に特化した 1200 × 750 px の branded 画像をブラウザ内で生成。
    * 画像にはスコア、ランクバッジ、詳細なランク説明文、および測定時の環境情報が含まれる。
  * **結果の共有機能 (Web Share API)**:
    * 測定完了後に「結果を共有」ボタンが活性化。
    * 画像付きでOS標準の共有メニューを呼び出し。非対応環境では画像をクリップボードに自動コピーし、投稿用URLを別タブで開くフォールバック機能を搭載。
    * 共有テキストには結果の数値とサイトURLが含まれる。
