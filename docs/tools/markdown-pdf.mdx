---
title: マークダウンPDF変換
slug: /markdown-pdf
sidebar_position: 15
hide_title: true
---

import MarkdownPdf from "@site/src/components/MarkdownPdf";
import ToolPageHeader from "@site/src/components/ToolPageHeader";
import { FileDown } from "lucide-react";

<ToolPageHeader
  title="マークダウンPDF変換"
  desc="アップロードしたマークダウンファイル（.md）や直接入力したテキストを、ブラウザ上で文字選択・検索可能なPDFに変換してダウンロードします。"
  icon={<FileDown size={48} color="#fff" />}
/>

<MarkdownPdf />

## 使い方

1. **マークダウンの入力**:
   * **ファイル読み込み**: `.md` ファイルを「ファイルを選択」ボタンから選択するか、ドラッグ＆ドロップして読み込みます。
   * **テキスト貼り付け**: テキストエリアにマークダウンテキストを直接貼り付け、または入力します。
2. **オプションの設定**:
   * 出力設定パネルから、フォントサイズ、余白（マージン）、用紙サイズ（A4/Letter）、用紙の向き（縦/横）、ページ番号表示の有無を調整します。
3. **プレビューの確認**:
   * **HTMLプレビュー**: リアルタイムでマークダウンのHTML変換結果を確認できます。
   * **PDFプレビュー**: 「PDFプレビュー生成」ボタンをクリックすると、指定したレイアウト設定に基づいたPDFのプレビューを下部に表示します。
4. **PDFのダウンロード**:
   * 「PDFダウンロード」ボタンをクリックすると、ブラウザ上でPDFを即時に生成し、直接ダウンロードできます。

:::info セキュリティについて
🔒 アップロードしたファイルや入力したテキストはすべてお使いのブラウザ内で処理されます。サーバーへ送信されることは一切ないため、機密情報が含まれるファイルでも安全にご利用いただけます。
:::
